import React from "react";

const Header = () => {
	return (
		<div className="w-full flex justify-center py-5 px-10 border-b">
			<span className="font-black text-3xl">Wordle</span>
		</div>
	);
};

export default Header;
